// cin/cout acceleration
//ios_base::sync_with_stdio(false);
//	to stop sync with standard input/output, which will turn off scanf/printf
//cin.tie(0);
//	to stop cin sync with cout, which means everything will not be printed until all cin operation finish
